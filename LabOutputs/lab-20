from sklearn.linear_model import LinearRegression

# Month
X = [
    [1], [2], [3],
    [4], [5], [6]
]

# Sales
y = [
    100, 120, 140,
    160, 180, 200
]

model = LinearRegression()

model.fit(X, y)

# Predict month 7
print("Future Sales:",
      model.predict([[7]])[0])
